{
  "//": [
    "Committed defaults only — nothing here is a particular of any operator's situation.",
    "Real values go in appsettings.Local.json (gitignored) or WEALTHOPS_* environment variables.",
    "appsettings.Example.json is the template to copy."
  ],
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft": "Warning",
      "WealthOps": "Warning"
    }
  }
}
